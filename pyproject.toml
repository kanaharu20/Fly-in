[project]
name = "fly-in"
version = "0.1.0"
description = "Drone routing simulator over a network of connected zones."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flake8>=7.3.0",
    "mypy>=2.3.0",
    "pydantic>=2.13.4",
]
